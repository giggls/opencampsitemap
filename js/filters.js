// Useful filters for UI
//
// It is fairly simple to add more filters using this syntax
//

var tag_filters = {
  'dog': [ 'yes', 'leashed' ],
  'fee': [ 'no' ],
  'power_supply': [ '^(?!no).+$' ]
}
